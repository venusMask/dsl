package org.venus.dsl.parse.node.value;

import org.venus.dsl.parse.node.Node;
import org.venus.dsl.parse.node.NodeLocation;

public abstract class ValueTakeNode extends Node {

    public ValueTakeNode(NodeLocation location) {
        super(location);
    }

}
